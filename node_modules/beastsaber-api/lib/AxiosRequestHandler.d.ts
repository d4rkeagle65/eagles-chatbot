import { AxiosInstance } from "axios";
import { CookieJar, Store } from "tough-cookie";
import { BsaberGetRequest, BsaberPostRequest, BsaberResponse, RequestHandler } from "./RequestHandler";
export declare class AxiosRequestHandler implements RequestHandler {
    domain: string;
    cookieJar: CookieJar;
    axios: AxiosInstance;
    constructor(domain: string, cookieStore?: Store);
    get({ endpoint, headers, }: BsaberGetRequest): Promise<BsaberResponse>;
    post({ endpoint, headers, params, }: BsaberPostRequest): Promise<BsaberResponse>;
}
