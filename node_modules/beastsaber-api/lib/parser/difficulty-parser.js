"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseDifficulties = void 0;
function parseDifficulties(difficulties) {
    return difficulties.map((difficulty) => {
        const url = difficulty.getAttribute("href");
        const index = url === null || url === void 0 ? void 0 : url.indexOf("=");
        const key = url === null || url === void 0 ? void 0 : url.substring(index + 1);
        const label = difficulty.text.trim();
        return {
            key: key,
            label: label,
        };
    });
}
exports.parseDifficulties = parseDifficulties;
