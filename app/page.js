import Sheet from "@mui/joy/Sheet"
import Grid from "@mui/joy/Grid"
import Box from "@mui/joy/Box"
import Typography from "@mui/joy/Typography"

export default function Home() {
  return (
    <Box sx={{ display: "flex", flexDirection: "column" }}>
      <p>Welcome to Eagles-Chatbot!</p>
      <p>This page is temporary to allow for the menu system to exist so that I could format the Queue page to display on mobile devices.</p>
      <p>More content will be added at a future date.</p>
      <p>If you are on mobile, click the hamburger menu icon on the top left to open the menu to find the queue.</p>
    </Box>
  )
}

