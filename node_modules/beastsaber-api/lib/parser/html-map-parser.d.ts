import { HTMLElement } from "node-html-parser";
import { Map } from "../models/Map";
export declare function parseMapElements(title: string, url: string, imageUrl: string, root: HTMLElement, meta: HTMLElement): Map | null;
